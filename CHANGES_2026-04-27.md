# Medicare API — cambios para subir al servidor

**Fecha de corte:** 2026-04-27
**Contexto:** trabajo de reschedule (26 abr) + migración de URLs time-slots y fix de validation/clinic_ids (27 abr).
**Acción:** subir estos archivos al servidor de producción (donde están registrados los certificados Bancard).

---

## ARCHIVOS NUEVOS — subir tal cual

| Path | Propósito |
|---|---|
| `app/Models/AppointmentRescheduleReqModel.php` | Modelo Eloquent del flow de reschedule. |
| `app/Http/Controllers/Api/V1/AppointmentRescheduleReqController.php` | Controller con `addRequest`, `approve`, `reject`, `deleteByUser`, `getDataByAppointmentId`, `getInitiatedList`. |
| `resources/views/emails/reschedule_requested.blade.php` | Template email cuando user crea request. |
| `resources/views/emails/reschedule_approved.blade.php` | Template cuando doctor aprueba. |
| `resources/views/emails/reschedule_rejected.blade.php` | Template cuando doctor rechaza. |
| `database/migrations/2026_04_26_120000_add_auto_reschedule_flags_to_doctors_table.php` | Migration que agrega 4 columnas a `doctors`. **Subir, pero NO correr** porque la DB usa SQL directo (los flags reales viven en `user_clinics`). Mantener para futuro `migrate:fresh`. |
| `database/migrations/2026_04_26_120100_create_appointment_reschedule_req_table.php` | Migration de la tabla. **Mismo criterio:** subir, no correr — usar el SQL `CREATE TABLE` que ya pasé. |
| `database/migrations/sql/update_v_doctors_view.md` | Doc de la regeneración de `v_doctors`. |
| `database/migrations/sql/fix_v_doctors_befor_typo.sql` | DDL del view con el typo corregido (`befor_minutes` → `before_minutes`). **Correr una vez en MariaDB del servidor.** |

## ARCHIVOS MODIFICADOS — subir reemplazando

| Path | Cambios principales |
|---|---|
| `app/Http/Controllers/Api/V1/AppointmentController.php` | • `getData` — filtro `clinic_ids` (CSV con whereIn / where=)<br>• `addData` — validation acepta `time` o `time_slots`, `type` o `appointment_type`; `patient_id` opcional cuando hay `user_id` (resolved server-side)<br>• Nuevo endpoint `userAppointmentReschedule` (guard de tiempo + fallback a request) |
| `app/Http/Controllers/Api/V1/AppointmentCancellationRedController.php` | `cancleAndRefund` — rama Bancard: si `id_payment_type=7500` + Paid + same-day-of-`payment_confirmed_at` → `cancelAndRollbackAppointmentPayment`. Si no, devuelve `redirect_to_reschedule:true`. |
| `app/Http/Controllers/Api/V1/DoctorController.php` | • `updateData` acepta los 4 flags `auto_rescheduled_*`<br>• `getDataById` los devuelve con defaults<br>• `getData` filtra por `user_id` (necesario para el combo del doctor-app) |
| `app/Http/Controllers/Api/V1/DoctorWeb/DoctorWebAppointmentController.php` | Filtro `clinic_ids` agregado al endpoint `/v1/doctor-web/appointments`. |
| `app/Http/Controllers/Api/V1/NotificationCentralController.php` | 3 nuevos métodos: `sendRescheduleRequestedNotification`, `sendRescheduleApprovedNotification`, `sendRescheduleRejectedNotification` + helper `safeSendMail`. |
| `app/Http/Controllers/Api/V1/TimeSlotsVideoController.php` | Eliminado método muerto `getDoctorClinicTimeInterval` (no estaba mapeado). |
| `routes/api.php` | • Borradas 4 rutas legacy `get_doctor_(video_)time_slots/{id}` y `get_doctor_(video_)time_interval/{id}/{day}`<br>• Agregadas 7 rutas nuevas para el flow de reschedule (`user_appointment_reschedule`, `appointment_reschedule_request`, `appointment_reschedule_request_approve/reject`, `delete_appointment_reschedule_request`, `get_appointment_reschedule_requests/{id}`, `get_initiated_reschedule_requests`)<br>• Import de `AppointmentRescheduleReqController` |

---

## SQL a correr en el servidor (en orden)

### 1. Crear tabla `appointment_reschedule_req`

```sql
CREATE TABLE IF NOT EXISTS `appointment_reschedule_req` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `appointment_id` BIGINT UNSIGNED NOT NULL,
  `status` ENUM('Initiated','Rejected','Approved','Processing')
           NOT NULL DEFAULT 'Initiated',
  `requested_date` DATE DEFAULT NULL,
  `requested_time_slots` VARCHAR(255) DEFAULT NULL,
  `notes` TEXT DEFAULT NULL,
  `reviewed_by_user_id` BIGINT UNSIGNED DEFAULT NULL,
  `reviewed_at` TIMESTAMP NULL DEFAULT NULL,
  `created_at` TIMESTAMP NULL DEFAULT NULL,
  `updated_at` TIMESTAMP NULL DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `arr_appointment_id` (`appointment_id`),
  KEY `arr_status` (`status`),
  KEY `arr_reviewed_by_user_id` (`reviewed_by_user_id`),
  CONSTRAINT `arr_appointment_id_fk`
    FOREIGN KEY (`appointment_id`) REFERENCES `appointments` (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_general_ci;
```

### 2. Recrear view `v_doctors` (corrige typo `befor_minutes`)

Correr el archivo:
```
database/migrations/sql/fix_v_doctors_befor_typo.sql
```

### 3. (Opcional, si querés tracking más rico de reschedules — propuesto, no obligatorio)

Las columnas que sugerí en `appointments` (`current_reschedule_req_status`, `reschedule_count`, `last_rescheduled_at`, `original_date`, `original_time_slots`) **no se aplicaron**. El código actual no las usa. Podés agregarlas más adelante si querés.

---

## Variables de entorno a verificar

- `MAIL_*` en `.env` deben estar configuradas (ya lo estaban para `SmtpController::sendForgetMail`). Los nuevos templates usan `Mail::send` con la misma config.
- `BANCARD_*` (return_url, cancel_url, base_url, retry_attempts) — sin cambios.

---

## Smoke test sugerido (cuando tengas el server actualizado)

1. **Reschedule request flow**:
   - User-app: agendar cita pagada → tap "Reschedule" → verificar que cae al request flow si está fuera del time-limit del doctor.
   - Verificar email entrante (mailable `reschedule_requested`).
   - Doctor aprueba via `/api/v1/appointment_reschedule_request_approve` → email de approved llega.

2. **Bancard same-day cancellation**:
   - Crear appointment con `id_payment_type=7500`, marcar Paid.
   - Cancelar dentro del mismo día → verificar log de `cancelAndRollbackAppointmentPayment` ejecutado.
   - Cancelar otro día → response debe traer `redirect_to_reschedule:true`.

3. **Time-slots URLs**:
   - User-app `flutter run --dart-define=VITE_CLINIC_ID=5` → al elegir doctor, log debe mostrar `/v1/doctors/5/clinics/5/time-interval/{day}` (no más `get_doctor_time_interval`).

4. **Add appointment**:
   - User-app: book OPD sin patient_id explícito → debe crear el patient via `resolveOrCreatePatientByUserIdForClinic` y la cita.

---

## Pendiente para mañana / próximo deploy

- Verificación end-to-end con cliente real (escanear QR Bancard si aplica).
- Si la prueba pasa, NO hace falta tocar nada más en el server hasta el próximo cambio.
- Si hay errores 500: revisar `storage/logs/laravel.log` — los nuevos métodos de email tienen `safeSendMail` que loggea warnings sin tirar el flow, y el cancel-and-refund tiene `Log::warning` para Bancard rollback failures.
